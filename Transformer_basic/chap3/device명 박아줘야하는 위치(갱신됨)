모델의 층
데이터로더 등으로 가져오는 각 데이터(텐서)
손실함수 클래스 criterion

데이터로더의 경우, 로더 클래스 선언에 넣는 매개변수의 텐서를 to(device)해도 되지만
주로 학습할 때 load한 다음 그걸 to(device)하더라, 호환성을 위해서인듯

nn.Module을 상속한 모든것을 선언할 때 해당 인스턴스에
init함수 내에서 선언 할 때도 마찬가지